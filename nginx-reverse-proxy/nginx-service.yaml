apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: backend
  labels:
    app: nginx
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - name: https
      port: 443
      targetPort: 443